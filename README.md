# Accounting Notebook frontend

## Starting the project.

In the project directory, you can run:

### `yarn start`

Runs the app in the browser. Automatically opens [http://localhost:3000](http://localhost:3000).
